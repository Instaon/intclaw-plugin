{
  "id": "insta-claw-connector",
  "name": "insta-claw-connector",
  "version": "1.0.16",
  "description": "A lightweight OpenClaw channel plugin for WebSocket-based bidirectional messaging using Open Responses protocol",
  "author": "Instagram Claw Team",
  "main": "index.ts",
  "channels": [
    "insta-claw-connector"
  ],
  "channelConfigs": {
    "insta-claw-connector": {
      "name": "insta-claw-connector",
      "description": "A lightweight OpenClaw channel plugin for WebSocket-based bidirectional messaging",
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "default": true
          },
          "clientId": {
            "type": "string",
            "description": "Client ID (x-app-key) for authentication with InstaClaw server"
          },
          "clientSecret": {
            "type": "string",
            "description": "Client Secret (x-app-secret) for authentication with InstaClaw server"
          },
          "systemPrompt": {
            "type": "string",
            "description": "System prompt for the InstaClaw connector"
          },
          "debug": {
            "type": "boolean",
            "description": "Enable debug logging",
            "default": false
          }
        }
      }
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "clientId": {
        "type": "string",
        "description": "Client ID (x-app-key) for authentication with InstaClaw server"
      },
      "clientSecret": {
        "type": "string",
        "description": "Client Secret (x-app-secret) for authentication with InstaClaw server"
      },
      "systemPrompt": {
        "type": "string",
        "description": "System prompt for the InstaClaw connector"
      }
    }
  }
}